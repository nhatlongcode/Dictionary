import React, { useState } from 'react';
import {Text, StyleSheet, View, TextInput, ToastAndroid, TouchableOpacity, useColorScheme, ScrollView, Button } from 'react-native';

const LandingScreen = () => {
    return(
        <View>
            <Text>This is landing screen</Text>
            <Button/>
        </View>
    );
};

const styles = StyleSheet.create({

})